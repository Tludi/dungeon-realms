<%= form_for(@monstertome) do |f| %>
  <% if @monstertome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monstertome.errors.count, "error") %> prohibited this monstertome from being saved:</h2>

      <ul>
      <% @monstertome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :room_id %><br />
    <%= f.text_field :room_id %>
  </div>
  <div class="field">
    <%= f.label :monster_id %><br />
    <%= f.text_field :monster_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
